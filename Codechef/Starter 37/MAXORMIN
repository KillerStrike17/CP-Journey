# cook your dish here
for _ in range(int(input())):
    N = int(input())
    A= list(map(int, input().split()))
    ones = A.count(1)
    zeros = A.count(0)
    if ones>=zeros:
        print(1)
    else:
        print(0)